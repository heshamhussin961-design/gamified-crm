flask==3.1.1
flask-cors==5.0.1
supabase==2.13.0
python-dotenv==1.1.0
openpyxl==3.1.5
requests==2.32.3
gunicorn==23.0.0

# --- AI (optional at runtime; lazy-imported) ---
anthropic==0.40.0
openai==1.54.0

# --- Billing (optional; lazy-imported when STRIPE_SECRET_KEY is set) ---
stripe==11.4.1

# --- Observability (optional; activated only when SENTRY_DSN is set) ---
sentry-sdk[flask]==2.18.0

# --- dev / tests ---
pytest==8.3.3
ruff==0.7.4
